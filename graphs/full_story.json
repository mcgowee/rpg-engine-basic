{
  "name": "full_story",
  "description": "Full story graph: narrator → (optional) mood → NPC → narrator coda → condense → memory. With characters, the narrator sets the scene only; mood updates, then NPC lines, then coda asks what you do. With no characters, routing skips mood, NPC, and coda and goes straight to condense. Use for character-driven stories with mood axes and rolling memory.",
  "nodes": ["narrator", "mood", "npc", "narrator_coda", "condense", "memory"],
  "entry_point": {
    "router": "route_graph_entry",
    "mapping": {
      "narrator": "narrator"
    }
  },
  "edges": [
    { "from": "mood", "to": "npc" },
    { "from": "npc", "to": "narrator_coda" },
    { "from": "narrator_coda", "to": "condense" },
    { "from": "condense", "to": "memory" },
    { "from": "memory", "to": "__end__" }
  ],
  "conditional_edges": [
    {
      "from": "narrator",
      "router": "route_after_narrator",
      "mapping": {
        "npc": "mood",
        "condense": "condense"
      }
    }
  ]
}
